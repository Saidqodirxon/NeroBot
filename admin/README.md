NeroBot Admin (React + Vite)

Quick start

1. Install deps

```bash
cd admin
npm install
```

2. Start dev server

```bash
npm run dev
```

3. Build for production

```bash
npm run build
```

Set `VITE_API_URL` in `.env` to point to your backend API (e.g. `https://your-domain.com/api/v1`).
